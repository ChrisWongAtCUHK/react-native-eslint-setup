# react-native-eslint-setup

## Objective
Set up React Native ESLint with minimal configuration.

## Steps
1. Generate eslintrc.json if necessary
2. Edit package.json
3. Install
4. VSCode setting(optional)
